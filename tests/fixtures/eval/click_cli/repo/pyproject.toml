[project]
name = "click-cli-demo"
version = "0.1.0"
description = "Demo project using Click 7.x"
requires-python = ">=3.8"
dependencies = [
    "click>=7.0,<8.0",
]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"
