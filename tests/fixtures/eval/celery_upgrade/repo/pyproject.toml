[project]
name = "celery-worker-demo"
version = "0.1.0"
description = "Demo project using Celery 4.x"
requires-python = ">=3.8"
dependencies = [
    "celery>=4.0,<5.0",
    "redis>=3.0",
]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"
