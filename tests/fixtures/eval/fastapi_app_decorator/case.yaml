case_id: fastapi_app_decorator
dependency: fastapi
source_version: "0.95"
target_version: "1.0"
skill_id: generic_python_dependency
with_docs: false
description: >
  FastAPI 0.95 -> 1.0: `FastAPI()` instantiation and `@app.get(...)` route
  decorators are stable; the case exercises a doc-less dependency (generic
  skill) where risks cannot reach VERIFIED without documentation evidence.
expect:
  conclusion: evidence_insufficient
  must_cite_paths:
    - src/app.py
  must_flag_symbols:
    - FastAPI
  max_severity: high
